---
# Inventory file for crypto-forecasting project
# Define your hosts and groups here

all:
  children:
    # Production environment
    production:
      hosts:
        # prod-web-01:
        #   ansible_host: 192.168.1.10
        #   ansible_user: ubuntu
        #   ansible_ssh_private_key_file: ~/.ssh/prod_key
        # prod-db-01:
        #   ansible_host: 192.168.1.11
        #   ansible_user: ubuntu
        #   ansible_ssh_private_key_file: ~/.ssh/prod_key
    
    # Staging environment
    staging:
      hosts:
        # staging-server:
        #   ansible_host: 192.168.1.20
        #   ansible_user: ubuntu
        #   ansible_ssh_private_key_file: ~/.ssh/staging_key
    
    # Development environment (local)
    development:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
    
    # Database servers
    database_servers:
      hosts:
        # prod-db-01:
        # staging-db:
        localhost:  # For local development
    
    # Web application servers
    web_servers:
      hosts:
        # prod-web-01:
        # staging-server:
        localhost:  # For local development
    
    # ML/AI processing servers
    ml_servers:
      hosts:
        # ml-server-01:
        localhost:  # For local development

  vars:
    # Global variables
    project_name: crypto-forecasting
    project_root: /home/palianm/Desktop/crypto-forecasting
    app_user: "{{ ansible_user | default('ubuntu') }}"
    app_group: "{{ ansible_user | default('ubuntu') }}"
    
    # Application settings
    app_port: 5000
    redis_port: 6379
    postgres_port: 5432
    
    # Docker settings
    docker_compose_version: "2.20.0"
    
    # Python settings
    python_version: "3.9"
    
    # Environment specific variables
    environment: development
